if [ -z "$METRICS_DIR_OVERRIDE" ]; then
  export METRICS_DIR="$METRICS_ROOT_DIR/$HOSTNAME"
else
  export METRICS_DIR="$METRICS_DIR_OVERRIDE"
fi

mkdir -p $METRICS_DIR
chown -R socrata:metrics $METRICS_DIR
chmod -R 774 $METRICS_DIR
chmod g+s $METRICS_DIR
